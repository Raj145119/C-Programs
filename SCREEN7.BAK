#include<stdio.h>
#include<conio.h>
#include<dos.h>
void main()
{
	int x=2,y=2,z=0;
	clrscr();
	p:
	if(z%2==0)
	textcolor(BLACK);
	else
	textcolor(z);
	z++;
	gotoxy(x,y);
	cprintf("PRAVEEN");
	y=y+4;
	if(y<=30)
	cprintf("PRAVEEN");

	y=2;
	x=x+4;
	if(x<=50)
	cprintf("PRAVEEN");
	x=50;
	y=y+4;
	if(y<=30)
	cprintf("PRAVEEN");
	x=x-4;
	if(x==2)
	y=30;
	cprintf("PRAVEEN");
	delay(200);
	if(kbhit()==0)
	goto p;
	getch();


}