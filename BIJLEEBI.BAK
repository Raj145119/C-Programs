#include<stdio.h>
#include<conio.h>
void main()
{
	int unit;
	float charge,charge1=0,total;
	clrscr();
	printf("\nenter the number in unit");
	scanf("%d",&unit);
	if(unit>0||unit<=100)

	{
	 charge=unit*2.80;
       //	 printf("%f",charge);
	}
       else if(unit>101||unit<=200)
	{
	 if(unit<=200)
	 charge1=charge+(unit-100)*3.10;
	 else
	 charge1=charge+100*3.18;
	 }
	 total=charge+charge1;
	 printf("charge=%f",total);
         getch();

}