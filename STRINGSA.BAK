#include<stdio.h>
#include<conio.h>

#include<string.h>
void main()
{
	int i;
	char st[10],st2[10];
	clrscr();
	printf("enter thee string");
	gets(st);
	gets(st2);
	i=strcmp(st,st2);
       //	i=stricmp(st,st2);
	if(i==0)
	printf("same string");
	else
	printf("different string");
	getch();
}