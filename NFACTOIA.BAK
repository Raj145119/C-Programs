#include<stdio.h>
#include<conio.h>
void main()
{
       int i,n;
       double a,sum=0.0,fact=1.0;
       clrscr();
       printf("\nEnter the number:");
       scanf("%d",&n);

       for(i=1;i<=n;i++)
       {
		fact=(fact*i);
		a=(i/fact);
	       //	fflush(stdout);
		printf("a   =%lf\n",a);

		sum=sum+a;

       }
		printf("%lf",sum);
	       //	return 0;
	getch();
}